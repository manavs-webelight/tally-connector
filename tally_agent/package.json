{
  "name": "tally_agent",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "concurrently \"npm run tauri -- dev\" \"cd src && python3 -m http.server 1420\"",
    "build": "npm run tauri build",
    "frontend:dev": "cd src && python3 -m http.server 1420"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "concurrently": "^9.0.0"
  }
}
